[general]
name = "IRIS"
campaign = "jepa_research"
environment = "research"

[storage]
root = "../var/iris"
datasets_dir = "../var/iris/datasets"

[jepa]
enabled = true
shadow_mode = true
dataset_path = "../var/iris/datasets/jepa"
embedding_dim = 256
encoder_layers = 6
attention_heads = 8
mask_fraction = 0.50
learning_rate = 0.0003
batch_size = 128
seed = 2026

