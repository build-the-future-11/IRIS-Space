# SIDEREA robust-search v2 cadence materialization diagnosis — 2026-09-09

Status: **pre-outcome reproducibility diagnosis; no v2 candidate-performance result is authorized by this document**.

Target baseline: PR #2 head `f1e6b23f1b48fa844c8dd4cde6e2ead36a846adb`.

## First causal CI failure

The Python matrix installs successfully and reaches the test suite. On the exact baseline above, the suite reports 448 passing tests and four failures. All four failures reduce to one predevelopment integrity break: the committed cadence materialization does not equal the output of the already-frozen cadence generator.

The frozen generator lock in `src/siderea/research/robust_protocol.py` requires:

- expected cadence-manifest SHA-256: `5abfdaa328f5793e92bed7aa6dcfaade44ac1a54a1996b8c310e51112dc72451`;
- frozen cadence seed: `2026091120`;
- 20 irregular plus 5 seasonal-gap cadence realizations, each with 64 epochs.

The committed `paper/experiments/robust_search_cadences.v2.json` instead has SHA-256:

`6ceb77227826eb9fc6430e400c9b18d27b83f93cc09cb5e1613df014d81fa4ef`.

Its Git blob at the diagnosed baseline is `c818b776f388a9f755cccafa1939f92bc5604e75`.

The failure is therefore not a Python-version, dependency-installation, or stochastic-test problem. The fail-closed gate is correctly refusing an input artifact that is inconsistent with its frozen generator.

## Permitted correction boundary

If repository history confirms that no v2 development, calibration, locked-evaluation, generalization-probe, or compound-nuisance performance result was generated or inspected after this inconsistent materialization, the scientifically conservative correction is:

1. regenerate the cadence JSON from the unchanged frozen protocol and unchanged generator;
2. require the regenerated bytes to hash to the already-frozen `5abfdaa328f5793e92bed7aa6dcfaade44ac1a54a1996b8c310e51112dc72451`;
3. replace only the incorrect materialized cadence artifact;
4. retain this diagnosis and the old artifact digest in history as a pre-result correction record;
5. rerun the public predevelopment verifier and the complete CI matrix before any candidate-performance runner is permitted to execute.

Do **not** change the frozen digest to match the currently committed file. Do not change the cadence seed, generator, protocol, amendment chain, trial allocation, endpoints, thresholds, or promotion gates to make CI pass.

## Separate mechanical failure

The Quality job also reports seven files that require `ruff format`. That is independent of the cadence integrity failure and may be corrected mechanically without changing scientific values.

## Scientific consequence

Until the cadence artifact is corrected and exact-head CI is green, robust-search v2 has no execution-ready predevelopment state. No candidate development, calibration, locked evaluation, or result interpretation should proceed from this branch.
